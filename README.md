# Portfolio

This is a simple static portfolio scaffold. Open `index.html` in your browser to view.

Quick start:

```bash
# from this folder
start index.html
```

Customize:
- Replace the placeholder text and images in `index.html`.
- Update the `mailto:` address in `script.js`.

To publish: upload the folder to GitHub Pages, Netlify, or Vercel.
